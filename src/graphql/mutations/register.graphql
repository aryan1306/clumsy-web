mutation Register($options: UserPasswordInput!) {
  register(options: $options) {
    user {
      id
      username
      email
      createdAt
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
